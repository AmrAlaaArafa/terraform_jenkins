pipeline {
    agent any
  tools {
    dockerTool 'Docker-Cli'
}
    stages {
        stage('Run Docker Container') {
            steps {
                script {
                    

                  

                    // Verify Docker
                    sh 'docker --version'

                
                }
            }
        }
    }
}
